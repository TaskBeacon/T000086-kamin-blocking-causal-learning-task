# Assets

This task uses PsychoPy text, rectangle, and textbox primitives only. Food cues are the concrete food names reported by Lovibond et al. (2003), configured in `config/*.yaml`; no external media are required.
